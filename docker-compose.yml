version: "3.9"

services:
  webapp:
    image: ghcr.io/frostoff92/frost-webapp:latest
    container_name: dev-frost-webapp
    ports:
      - "5000:5000"
    restart: unless-stopped
    networks:
      dev_lab_net:
        aliases:
          - webapp

networks:
  dev_lab_net:
    external: true
